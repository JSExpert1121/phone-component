import { NumfilterDirective } from './numfilter.directive';

describe('NumfilterDirective', () => {
	it('should create an instance', () => {
		const directive = new NumfilterDirective();
		expect(directive).toBeTruthy();
	});
});
